import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.nybroans.vinslipp",
  appName: "Vinslipp",
  webDir: "out",
  server: {
    url: "https://doguzuyar.github.io/vinslipp",
  },
  ios: {},
};

export default config;
